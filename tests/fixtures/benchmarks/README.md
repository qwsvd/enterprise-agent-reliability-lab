# Synthetic benchmark contract fixtures

These small files are owned by this repository and exist only to test the
adapter contract. They are not copied τ³-bench tasks, datasets, trajectories,
or official Sierra benchmark results, and must not be cited as benchmark scores.
